function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 

var numerosRandom;
var min;
var max;

min = 1;
max = 10;

numerosRandom = Math.floor(Math.random() * (max - min)) + min;

if (numerosRandom >= 9) {
	alert("EXCELENTE" + numerosRandom);
 } 
   else {
	   if (numerosRandom >= 4){
		   alert("APROBO" + numerosRandom);
	   }
	else {
		if (numeroRandom )
	}

   }











}//FIN DE LA FUNCIÓN