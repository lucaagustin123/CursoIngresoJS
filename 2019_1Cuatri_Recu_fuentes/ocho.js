function mostrar()
{ 
    var nombre;
    var cantidadDePersonas;
    var cantidadDeDias;
    var formasDePago;
    var respuesta;
    var contadorDeReservas;
    var acumuladorDeReservas;
    var maximoPersonas;
    var 
    var
    var 

    respuesta ="s";
    while (respuesta == "s")
    {
    nombre= prompt("ingrese nombre");
    while(!isNaN(nombre))
    {
        nombre=prompt("ingrese nombre");
    }
    cantidadDePersonas=prompt("cantidad de personas");
    parseInt(cantidad);
    while(isNaN(cantidadDePersonas) || cantidadDePersonas<1)
    {
     cantidadDePersonas=prompt("cantidad de personas");
     parseInt(cantidadDePersonas);
    }
     while(isNaN(cantidadDeDias) || cantidadDeDias<1)
    {
     cantidadDeDias=prompt("cantidad de dias");
     parseInt(cantidadDeDias);
    }
     while(!isNaN(formasDePago) || formasDePago !="efectivo" || formasDePago !="tarjeta" || formasDePago !="qr")
    {
     formasDePago=prompt("forma de pagos");
     parseInt(formasDePago);
    } 
     switch
    } respuesta = prompt("desea continuar");
    if

}
